import 'package:flutter/material.dart';

class Adminprofile extends StatelessWidget {
  const Adminprofile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("A profile"),
      ),
    );
  }
}
